---
# configure_net-snmp_agent/defaults/main.yml

# path to /var/lib/net-snmp
# needs to be set differently on Debian systems later
path_var_lib_snmp: '/var/lib/net-snmp'

snmpd_service_name: 'snmpd'
snmp_authentication_encryption_method: 'SHA'
snmp_privacy_encryption_method: 'AES'
